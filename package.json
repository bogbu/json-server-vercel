{
  "name": "json-server-vercel",
  "version": "1.0.0",
  "description": "Deploy JSON Server to Vercel",
  "main": "api/server.js",
  "scripts": {
    "dev": "json-server-auth --watch db.json --port 4444 -r routes.json"
  },
  "dependencies": {
    "json-server": "^0.17.4"
  },
  "devDependencies": {
    "json-server-auth": "^2.1.0"
  }
}
